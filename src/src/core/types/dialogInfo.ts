export interface IInfoDialog {
    lastRecord: string,
    lastRecordString: string | null,
    lastExecutionCommandDate: string,
    sourceCount: number,
    destinationCount: number,
    isSuccess: boolean
}